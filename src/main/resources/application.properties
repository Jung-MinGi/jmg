

spring.datasource.url=jdbc:mysql://${RDS_HOSTNAME}:${RDS_PORT}/${RDS_DB_NAME}?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
spring.datasource.username=${RDS_USERNAME}
spring.datasource.password=${RDS_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port = 5000

#cloud.aws.credentials.access-key=${cloud.aws.credentials.access-key}
#cloud.aws.credentials.secret-key=${cloud.aws.credentials.secret-key}
#cloud.aws.s3.bucket=${cloud.aws.s3.bucket}
#cloud.aws.region.static=${cloud.aws.region.static}
#cloud.aws.stack.auto=false
#cloud.aws.s3.upload-tempPath=image/temp/
#cloud.aws.s3.upload-Path=image/